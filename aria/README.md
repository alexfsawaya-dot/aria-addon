# Aria (Home Assistant add-on)

Runs the Aria dashboard as an always-on add-on alongside Home Assistant. Prebuilt
image pulled from GHCR; talks to Core locally over the Supervisor. Set a **setup
code** in Configuration before first start, then open `http://<hub-ip>:3000`.

Source + build pipeline live in the private `aria-web` repository.
