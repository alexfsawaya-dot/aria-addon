# Aria — Home Assistant add-on repository

Public distribution repo for the **Aria** Home Assistant add-on (manifest only —
no source). Add this repo's URL in Home Assistant → Apps → App store → ⋮ →
Repositories, then install **Aria**. The add-on pulls a prebuilt image from GHCR.
